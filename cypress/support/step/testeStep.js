import testePage from "../page/testePage";

const teste = new testePage()

Given(/^que acesso a tela de login$/, () => {
    teste.acessarsite()
});

And(/^informo os dados validos$/, () => {
    
});

When(/^valido se o login é realizado com sucesso$/, () => {
  
});